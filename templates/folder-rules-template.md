# 00_使用规则 — {文件夹名}

> 本文件夹由 folder-caretaker 托管（生成时间：{YYYY-MM-DD}）。
> 🔴 任何 Agent 读取/写入/删除本文件夹内容前，必须阅读并遵守本规则。
> 本文件以 `00_` 前缀命名，列目录时排在最前——进场即读。

---

## 这是什么

{2-3 句话说明文件夹的用途与内容定位}

### 目录地图

```
{文件夹名}/
├── {子目录1}/          ← {用途}
├── {子目录2}/          ← {用途}
├── (散落文件…)
└── 00_使用规则.md      ← 本文件
```

---

## 放置规则（🔴 硬性）

| # | 规则 | 说明 |
|---|------|------|
| 1 | **新产物放哪** | {明确路径规则，如：代码放 src/，文档放 docs/} |
| 2 | **命名规则** | {如：简洁中文名；文件名不带日期版本；日期只放 frontmatter} |
| 3 | **文件内部规范** | {如：md 文件必须有 frontmatter（标题/日期/标签）} |
| 4 | **不做什么** | {如：不裸放根目录；不建无内容空文件夹} |

---

## 禁区（🔴 不可违反）

- 不删除用户手动创建的文件/结构
- 不重组本目录结构（除非用户明确要求）
- 不移动正在被其他程序占用的文件
- {按需追加该文件夹特有禁区}

---

## 交接与依赖方向（🔴 项目 / 协作类文件夹适用）

> 多会话 / 多 Agent 协作的文件夹必须包含本协议（出处：project-vault STANDARDS §16「记忆与交接」）；纯素材/一次性文件夹可省略。

| # | 规则 | 说明 |
|---|------|------|
| 1 | **新会话入口** | 本文件夹固定一份交接文档（如 `04-执行状态/交接文档.md`），内容五要素：一句话定位 / 现状 / 待拍板（合并全部未决项）/ 下一步 / 外部指针 |
| 2 | **待拍板合并** | 所有未决问题统一收进交接文档，不散落各文件 |
| 3 | **依赖单向** | 文件引用只朝一个方向：内部 → 细节文件 / 外部 → 产品仓指针；**禁止反向引用与循环依赖** |
| 4 | **指针不镜像** | 外部内容（代码 / 成品 / 原件）只记位置指针，不复制内容进本文件夹 |

---

## 维护日志

| 日期 | 操作 | 执行者 |
|------|------|--------|
| {YYYY-MM-DD} | 生成本规则 | folder-caretaker |
