# 00_使用规则 — {文件夹名}

> 本文件夹由 folder-caretaker 托管（生成时间：{YYYY-MM-DD}）。
> 🔴 任何 Agent 读取/写入/删除本文件夹内容前，必须阅读并遵守本规则。
> 本文件以 `00_` 前缀命名，列目录时排在最前——进场即读。

---

## 这是什么

{2-3 句话说明文件夹的用途与内容定位}

### 目录地图

```
{文件夹名}/
├── {子目录1}/          ← {用途}
├── {子目录2}/          ← {用途}
├── (散落文件…)
└── 00_使用规则.md      ← 本文件
```

---

## 放置规则（🔴 硬性）

| # | 规则 | 说明 |
|---|------|------|
| 1 | **新产物放哪** | {明确路径规则，如：代码放 src/，文档放 docs/} |
| 2 | **命名规则** | {如：简洁中文名；文件名不带日期版本；日期只放 frontmatter} |
| 3 | **文件内部规范** | {如：md 文件必须有 frontmatter（标题/日期/标签）} |
| 4 | **不做什么** | {如：不裸放根目录；不建无内容空文件夹} |

---

## 禁区（🔴 不可违反）

- 不删除用户手动创建的文件/结构
- 不重组本目录结构（除非用户明确要求）
- 不移动正在被其他程序占用的文件
- {按需追加该文件夹特有禁区}

---

## 维护日志

| 日期 | 操作 | 执行者 |
|------|------|--------|
| {YYYY-MM-DD} | 生成本规则 | folder-caretaker |
