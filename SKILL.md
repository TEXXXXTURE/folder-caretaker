---
name: folder-caretaker
description: 管理/整理任意文件夹时用：扫描并生成根目录 00_使用规则.md 声明（持续治理），或一次性有逻辑地梳理文件夹。
version: 1.0.0
platforms: [windows, linux, macos]
license: MIT
metadata:
  hermes:
    tags: [folder, management, organization, caretaker, 文件夹, 整理, 治理]
    related_skills: [windows-filesystem-management, knowledge-base-management, session-library-triage]
---

# Folder Caretaker — 文件夹自治管理

## When to Use

- 用户说「管理这个文件夹 / 给这个文件夹定规则 / 以后按规矩放」→ 功能 A（持续治理）
- 用户说「整理一下这个文件夹 / 这个文件夹好乱 / 梳理一下」→ 功能 B（一次性梳理）
- 任何 AI 产出物文件夹、代码库、知识库、素材库、会话导出、混合杂物堆
- 注意：应用目录（如 D:\软件）默认只生成声明，不做重组

## 核心理念（为什么存在）

**AI 与 Agent 擅长产出内容，却对自己的产出没有结构感**：生成的文件默认散落，无命名约定、无归类逻辑、无归档规则；用户不会主动整理 AI 生成的产物。不治理的 AI 产出物只会越积越乱，最终变成无法检索的垃圾堆。

本 skill 给 Agent 的产出建立结构——让 AI 生成的东西像人整理过的一样有逻辑、可检索、可复用。

## 双功能

| 功能 | 触发 | 动作 | 产物 |
|------|------|------|------|
| A. 持续治理 | 用户说"管理这个文件夹" | 扫描 → 生成 `00_使用规则.md` 声明 → 登记 | 文件夹根目录声明 + 进场协议生效 |
| B. 一次性梳理 | 用户说"整理一下这个文件夹" | 扫描 → 提出整理方案 → 确认 → 执行 | 实际目录结构变动 |

## 机制原理（进场协议）

**约束性来自进场协议铁律，不来自文件本身**（AGENTS.md/SKILL.md 的注入只在 cwd 恰好在文件夹内时生效；桌面聊天 cwd 恒在 home，声明文件不会自动注入）：

- 根路由模板已写入铁律：读取/写入/删除任何文件夹前，先查根目录有无 `00_使用规则.md` / `AGENTS.md` / `.agent-rules/`，有则必读必守
- 规则文件以 `00_` 前缀命名，列目录即见——**进场即读**
- Agent 只有在存取文件夹时与之打交道，进场即读规则是"文件夹自治"唯一生效点

## 执行流程

### 功能 A：持续治理（生成声明）

1. **扫描**：列目标文件夹树（深度 ≤3，跳过 `.git`/`node_modules`/`__pycache__` 等），统计文件类型分布
2. **识别类型**：按内容特征匹配类型模板（见下）
3. **起草声明**：生成 `00_使用规则.md`（按对应类型模板填充）
   - 必须包含：这是什么（目录地图）/ 放置规则（硬性）/ 禁区（不可违反）/ 维护日志
4. **用户确认**：展示声明草案，确认后写入文件夹根目录
5. **登记**：在文件夹内 `.agent-rules/` 记录生成时间与版本（可选）

### 功能 B：一次性梳理（整理文件夹）

1. **扫描**：完整列目录 + 文件类型统计 + 语义归类
2. **提出方案**：给出整理计划（新建分类目录 / 移动映射表 / 重命名建议），**枚举确认，不直接动手**
3. **执行**：用户确认后，按方案执行（移动/重命名/建目录）
4. **善后**：清理空目录，生成 `00_使用规则.md` 防止再次失序（与功能 A 衔接）

## 类型适配模板

| 类型 | 识别特征 | 声明侧重 |
|------|---------|---------|
| 代码项目 | package.json / pyproject.toml / src/ / tests/ | 结构约定、构建命令、命名规则 |
| 文档输出 | .md/.docx/.pdf 为主，无代码 | 命名规则、归档规则、frontmatter 规范 |
| 素材库 | 图/音/视频多，按类型分目录 | 分类约定、命名模板、元数据 |
| 会话导出 | 时间戳文件名、session 字样的 md | 归类规则、摘要索引约定 |
| 混合杂物堆 | 无主导类型 | 先分类后治理、分区放置 |

## 禁区（默认写进所有声明，用户可增删）

- 不删除用户手动创建的文件/结构
- 不重组已存在的目录结构（除非用户明确要求）
- 不移动正在被其他程序占用的文件（应用目录）
- 技术目录（node_modules/.git 等）不整理

## Pitfalls

- **不要主动修改已有文件内容**——只移动/重命名/建目录，内容留给用户或后续任务
- **功能 B 必须先出方案再动手**（操作手册 §3 枚举确认），禁止直接执行整理
- **声明是给所有 agent 看的**——语言用通用规范（中文+必要英文），不写"我"、"这个 agent"
- **识别类型不确定时问用户**，不要猜（操作手册 §1）
- **应用文件夹（如 D:\软件）默认只生成声明不做功能 B**——用户明确不接受重组（历史教训）
